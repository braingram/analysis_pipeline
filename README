A rewrite of a generic analysis pipeline that:

1) has connectivity defined by a networkx graph
2) has nodes defined by a cconfig config file

needs:

1) results cascading
2) easy partial running (for init)
3) iterative running (for chunks)
